# pharma-ci-engine — runtime + dev dependencies (Python 3.11+)
# Ask before adding anything not listed here (see CLAUDE.md).

openai            # LLM + text-embedding-3-* (structured outputs)
faiss-cpu         # vector store
rank-bm25         # keyword leg of hybrid retrieval
pydantic>=2       # domain schema + structured outputs (v2)
pdfplumber        # PDF text/table extraction
httpx             # downloads + live API clients (ClinicalTrials.gov, FDA)
pytest            # tests + eval harness
